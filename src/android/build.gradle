repositories{
    jcenter()
    flatDir{
        dirs 'aar'
    }
}
dependencies {
    compile(name:'yello_framework', ext:'aar')
}
android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}